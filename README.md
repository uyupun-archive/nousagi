# nousagi

🐰 geolocation tracking tool 🥕

### Build environment

```bash
$ yarn install
$ brew install ngrok
```

### Start server

```bash
$ yarn start    // localhost:3000 or on your network
$ yarn ext      // external link(ngrok)
```
